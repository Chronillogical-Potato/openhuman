{
  "$comment": [
    "Records in `modules::registry::ALL` whose submodule pin deliberately does",
    "NOT sit on the tag their `version` names. See scripts/ci/check-module-pins.mjs.",
    "",
    "An entry here is a claim that the drift is known and accepted, NOT a way to",
    "silence the gate: `expect` pins the exact `git describe --tags` output, so a",
    "record already exempt cannot drift FURTHER without failing. Widening the drift",
    "is a deliberate edit to this file, which is a reviewable diff.",
    "",
    "Delete an entry the moment the pins are reconciled. An exemption that has",
    "stopped being true fails the gate too \u2014 `expect` must still match."
  ],
  "exemptions": [
    {
      "id": "tinyjuice",
      "submodule": "vendor/tinyjuice",
      "expect": "v0.3.2-3-g69622d76",
      "reason": "The source is three CI and toolchain commits past v0.3.2; the bus contract is unchanged and the registry loads the published v0.3.2 artifact."
    },
    {
      "id": "tinyruntime",
      "submodule": "vendor/tinyruntime",
      "expect": "v0.2.5-5-g1f41a4d1",
      "reason": "The source is five CI and toolchain commits past v0.2.5; the bus contract is unchanged and the registry loads the published v0.2.5 artifact."
    },
    {
      "id": "tinyruntime-nodejs",
      "submodule": "vendor/tinyruntime",
      "expect": "v0.2.5-5-g1f41a4d1",
      "reason": "The provider is published independently at v0.2.2 while the shared tinyruntime source is five CI and toolchain commits past v0.2.5; registry retains its published provider artifact."
    },
    {
      "id": "tinyruntime-python",
      "submodule": "vendor/tinyruntime",
      "expect": "v0.2.5-5-g1f41a4d1",
      "reason": "The provider is published independently at v0.2.2 while the shared tinyruntime source is five CI and toolchain commits past v0.2.5; registry retains its published provider artifact."
    },
    {
      "id": "tinyconnectors",
      "submodule": "vendor/tinyconnectors",
      "expect": "v0.10.1-7-ge5c934c3",
      "reason": "The source is seven CI, formatting, and toolchain commits past v0.10.1; the bus contract is unchanged and the registry loads the published v0.10.1 artifact."
    }
  ]
}
