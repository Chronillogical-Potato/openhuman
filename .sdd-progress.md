Objective: migrate host-independent agent runtime/helpers into TinyAgents family with direct imports and no compatibility shims.
Base: migrate-inference-to-tinyinference @ 8c90600688a143e8bd377a2296db5d626bea5095; upstream/main merged at 6b88a59930257a9cc310c34e78a1cf9fbdc8d0b7.
Plan: docs/plans/migrate-agent-runtime-to-tinyagents.md
Spec: docs/specs/agent-runtime-upstream-boundary.md

Audit: complete (OpenHuman agent tree + TinyAgents/TinyTools/TinyInference capabilities reviewed).
Documentation: complete (boundary spec + executable plan + helper packages + ownership waves; final review PASS with no findings).
Profiles and session/orchestration audits: complete. Executable TDD plans are
`docs/plans/migrate-agent-sessions-to-tinyagents-session.md` and
`docs/plans/migrate-agent-orchestration-to-tinyagents.md`; implementation is
pending. They require direct imports with no compatibility shims and preserve
OpenHuman policy/security, persistence-root wiring, BUS/progress/RPC projection,
and concrete execution adapters.
Wave 2: complete (direct canonical imports, explicit recursive context, execution-scoped graph bindings including durable resume/retry, registry/definition split, integration fixtures, and full workspace gate; independent Terra reviews resolved).
Wave 3: complete in TinyAgents (all ten host capabilities, truthful unary/stream terminal lifecycle, parent-authority-preserving recursion, budget compression/accounting, and upstream Claude provider merge; independent Terra Gate 3 and merge reviews PASS).
Wave 4 Phase A: complete at OpenHuman commit 9baad52086 (canonical direct imports, behavior-free facade deletion, host-only conversion/tracing seams retained; independent Terra review PASS).
Wave 4 Phase B1: complete through OpenHuman commit a3dcf3f897 (explicit OpenHumanRunContext foundation, ten-capability host bundle, canonical shared-tool adapter, call-identity-safe middleware migration, truthful routing-role projection, and boundary baseline reduced from 377 to 358; independent Terra re-review PASS). The context is intentionally not live yet; Phase B2 owns task-local and route cutover.
Wave 4 Phase B2g: complete (root `RunContext<OpenHumanRunContext>` now carries the shared dispatch guard, attachment placeholders, and explicit child-usage ledger; synchronous descendants read and write those handles directly, `LastTurnUsage` is populated from the root ledger, and the three ambient turn-local modules are deleted). The guard keeps its host-owned pause and wall-clock policy, while deterministic tests cover pause/budget refusal and sibling-ledger isolation.
Wave 4 Phase C1: complete (the production root `ChatTurnGraph` now fails closed without retained hosted authority and otherwise enters the one process-shared `AgentHarness` through an invocation-local `AgentInvocation`/`InvocationRuntime`. The session's frozen prompt/context ladder remains the request, while the per-turn model route set, tools, policy session, progress, workspace, cancellation, steering, journal, artifacts, budgets, and terminal mapping stay invocation-local. Channel and sub-agent routes remain on the explicitly separate legacy runner. The exact boundary baseline is now 210, down by two root-route bridge entries.)
Task -1: complete (git_attribution removed; focused behavioral Git-hardening test passed; re-review spec PASS and quality PASS with no findings).
Task 0: complete (temporary exact baseline reduced from 455 to 377 after Phase A; checker scans all OpenHuman crates and TinyAgents facades; vendored dependency boundary 6/6; Terra re-review PASS).
Task 1: in progress (TinyTools final is merged `7a40010` in PR #6; the source-breaking/tool-controlled trust experiment was removed and empty PR #7 closed; TinyAgents and OpenHuman direct-import consumers complete; final runtime deletion remains).
Task 2: in progress (TinyAgents and OpenHuman use tinytools-agent directly and deleted tool_calling/pformat/parse/dispatcher facades; final manual runtime deletion remains).
Task 3: in progress (TinyInference final reviewed head `ecfdb76`, upstream PR #13 approved with core CI green; TinyAgents consumers complete; OpenHuman decorator/task-local deletion remains).
Task 4: in progress (TinyAgents explicit context, child data policy, graph binding and durable continuation complete; OpenHuman task-local removal remains).
Task 5: in progress (TinyAgents host-driven harness complete and Gate 3 PASS; OpenHuman ten-capability factory and explicit context foundation complete, with live route integration in Phase B2).
Task 6: in progress (tinyagents-definition and direct registry consumers complete; OpenHuman cutover remains).
Task 7: in progress (canonical graph/delegation lifecycle and tests complete; OpenHuman cutover remains).
Task 8: pending.
Task 9: pending.
Task 10: pending.
Task 11: pending.
Task 12: pending.

Upstream handoff:
- TinyTools: merged PR #6 at `7a40010`.
- TinyInference: ready PR #13 at reviewed head `ecfdb76`; approved, unresolved review threads 0.
- TinyAgents: ready PR #154 at `696379d`; review threads 0; harness tests, focused integration tests, workspace check, and default/all-feature strict Clippy pass; final independent delta review and upstream CI rerun pending.

Decisions:
- Direct imports only; no forwarding modules, type aliases, or compatibility re-exports for migrated APIs.
- OpenHuman keeps product policy, security, durable wire formats, persistence, RPC/UI projection, configuration, and concrete host adapters.
- Do not add tinyagents-inspect in this migration; diagnostics remain a later independently justified extraction.
- Specialized tdd-implementer/code-reviewer roles were unavailable; use scoped worker agents and read-only explorer reviewers while preserving the same implementation/review gates.

Wave 1 bases:
- TinyTools: 1eb3dade18b56c67dacbe16ae92ce42b0a097b7a
- TinyInference: 6db1968d1463791e1823c808808da64b9579b8fc

Wave 2 gate evidence:
- `cargo fmt --all -- --check` passed in TinyAgents.
- `cargo test --workspace` passed in TinyAgents.
- `cargo clippy --workspace --all-targets -- -D warnings` passed in TinyAgents.
- `pnpm rust:layout`, `pnpm docs:check`, the agent-runtime boundary checker, and the dependency-boundary suite passed.
